CREATE CAST (character varying AS color) with inout as assignment;
CREATE CAST (character varying AS country) with inout as assignment;
CREATE CAST (character varying AS dragon_character) with inout as assignment;